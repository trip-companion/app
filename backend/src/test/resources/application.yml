mongock:
  enabled: false

spring:
  data:
    mongodb:
      database: trip-companion
      port: 30000

jwt:
  secret: ${random.uuid}
  refreshTokenExpirationInMs: 1209600000
  accessTokenExpirationInMs: 900000